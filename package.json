{
  "name": "tdd-clean-node-api",
  "version": "1.0.0",
  "description": "API oriented to TDD and Clean Architecture",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "*.js":["standard --fix", "git add"]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "author": "matheus",
  "license": "ISC",
  "devDependencies": {
    "lint-staged": "^11.0.0",
    "standard": "^16.0.3"
  }
}
